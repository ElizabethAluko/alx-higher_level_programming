#!/usr/bin/python3

def add_tuple(tuple_a=(), tuple_b=()):
    tuple = ()
    for i in range(0, 2):
        if tuple_a[i] == NULL:
            tuple_a[i] = 0
    for i in range(0, 2):
        if tuple_a[i] == NULL:
            tuple_a[i] = 0
    tuple = (tuple_a[0] + tuple_b[0], tuple_a[1] + tuple_b[1])
    return (tuple)
